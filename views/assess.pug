extends layout

block content
  section
    h2 #{section}

    nav
      ul
        each article in articles
          li 
            a(href=`#${article.heading}`) #{article.heading}

    each article in articles
      article
        h3 #{article.heading}
        each link in article.links
          p #[a(href=link.href)= link.title]
        