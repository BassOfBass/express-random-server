//- semantic `<blockquote>` which also exposes the `<cite>` URL
mixin semQuote(url, source)
  .quotewrapper
    p As per
      a(href=url) #[cite #{source}]
      .
        :
    blockquote(cite=url)
      block quote